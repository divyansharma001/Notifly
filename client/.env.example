# Copy to client/.env for host dev. This file IS committed; .env is not.
# Read in vite.config.ts (Node) and sent as auth headers on proxied /v1 calls —
# never exposed to the browser. Must match the server's seeded dashboard service.
DASH_APP_KEY=nfy_key_dashboard
DASH_APP_SECRET=nfy_secret_dashboard_dev
